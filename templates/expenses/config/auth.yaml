# Email + password auth. First registered user becomes admin (Better Auth
# firstUserAdmin); subsequent sign-ups get the default `member` role. Members
# file expenses; admins review them (and bypass the row-level scoping below).
allowSignUp: true
defaultRole: member
strategies:
  - type: emailAndPassword
    minPasswordLength: 8
