# Reusable team top-nav for the authenticated pages.
type: container
element: header
props:
  className: border-b
children:
  - type: container
    props:
      className: max-w-6xl mx-auto px-6 py-3 flex items-center justify-between
    children:
      - type: container
        props:
          className: flex items-center gap-6
        children:
          - type: text
            element: span
            props:
              className: text-base font-semibold
            content: Events
          - type: container
            props:
              className: flex items-center gap-1 text-sm
            children:
              - type: link
                props:
                  href: /
                  className: px-3 py-1.5 rounded-md font-medium
                content: Public page
              - type: link
                props:
                  href: /calendar
                  className: px-3 py-1.5 rounded-md font-medium
                content: Calendar
              - type: link
                props:
                  href: /registrations
                  className: px-3 py-1.5 rounded-md font-medium
                content: Registrations
      - type: button
        props:
          className: px-3 py-1.5 rounded-md border text-sm font-medium
        content: Sign out
        action:
          type: auth
          method: logout
          onSuccess:
            navigate: /
