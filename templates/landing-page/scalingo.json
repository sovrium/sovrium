{
  "name": "Sovrium Landing Page",
  "description": "A bilingual marketing site with reusable components.",
  "repository": "https://github.com/sovrium/landing-page-template",
  "logo": "https://sovrium.com/logo.png",
  "website": "https://sovrium.com/apps/landing-page",
  "env": {
    "BUILDPACK_URL": {
      "description": "Sovrium buildpack — downloads the released, checksum-verified sovrium binary and boots the app config (Scalingo does not build Dockerfiles).",
      "value": "https://github.com/sovrium/scalingo-buildpack",
      "required": true
    },
    "SOVRIUM_ENCRYPTION_KEY": {
      "description": "Master key for encrypting stored credentials at rest (AES-256-GCM). Optional — the app generates and persists one on first start if unset. Keep it set here: the container filesystem is ephemeral, so a generated key would not survive a restart.",
      "generator": "secret",
      "required": false
    },
    "BASE_URL": {
      "description": "Public URL of this app (e.g. https://my-landing-page.osc-fr1.scalingo.io). Used for auth callbacks and absolute links.",
      "required": true
    }
  },
  "addons": [
    {
      "plan": "postgresql:postgresql-starter-512"
    }
  ],
  "formation": {
    "web": {
      "amount": 1,
      "size": "S"
    }
  }
}
