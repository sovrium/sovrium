{
  "$id": "maxItems.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Maximum number of items allowed",
  "description": "Maximum number of items allowed",
  "type": "integer",
  "minimum": 1,
  "x-specs": [
    {
      "id": "APP-FIELD-ARRAY-MAXITEMS-001",
      "given": "user provides maxItems >= 1",
      "when": "validating input",
      "then": "value should be accepted",
      "validation": {
        "setup": {
          "fieldConfig": {
            "name": "example_field",
            "type": "text"
          }
        },
        "assertions": [
          {
            "description": "value should be accepted",
            "validateConfig": true,
            "expectError": null
          }
        ]
      }
    },
    {
      "id": "APP-FIELD-ARRAY-MAXITEMS-002",
      "given": "user provides maxItems < 1",
      "when": "validating input",
      "then": "error should enforce minimum value",
      "validation": {
        "setup": {
          "fieldConfig": {
            "name": "example_field",
            "type": "text"
          }
        },
        "assertions": [
          {
            "description": "error should enforce minimum value",
            "validateConfig": true,
            "expectError": null
          }
        ]
      }
    }
  ]
}
