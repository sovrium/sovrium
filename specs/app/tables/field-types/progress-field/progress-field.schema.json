{
  "$id": "progress-field.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Progress Field",
  "type": "object",
  "properties": {
    "id": {
      "$ref": "../../../common/definitions.schema.json#/definitions/id"
    },
    "name": {
      "$ref": "../../../common/definitions.schema.json#/definitions/name"
    },
    "required": {
      "$ref": "../common/required/required.schema.json"
    },
    "type": {
      "type": "string",
      "const": "progress"
    },
    "color": {
      "$ref": "./color/color.schema.json"
    }
  },
  "description": "Visual progress bar (0-100%)",
  "required": ["id", "name", "type"],
  "additionalProperties": false,
  "x-specs": [
    {
      "id": "APP-PROGRESS-FIELD-001",
      "given": "entity with type='progress'",
      "when": "processing configuration",
      "then": "correct handler should be selected",
      "validation": {
        "setup": {
          "fieldConfig": {
            "name": "example_field",
            "type": "text"
          }
        },
        "assertions": [
          {
            "description": "correct handler should be selected",
            "validateConfig": true,
            "expectError": null
          }
        ]
      }
    },
    {
      "id": "APP-PROGRESS-FIELD-002",
      "given": "invalid type value",
      "when": "validating configuration",
      "then": "clear error message should identify the issue",
      "validation": {
        "setup": {
          "fieldConfig": {
            "name": "example_field",
            "type": "text"
          }
        },
        "assertions": [
          {
            "description": "clear error message should identify the issue",
            "validateConfig": true,
            "expectError": null
          }
        ]
      }
    }
  ]
}
