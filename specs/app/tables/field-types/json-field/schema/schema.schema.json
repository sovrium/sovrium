{
  "$id": "schema.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Optional JSON Schema to validate the data",
  "description": "Optional JSON Schema to validate the data",
  "type": "object",
  "x-specs": [
    {
      "id": "APP-FIELD-JSON-SCHEMA-001",
      "given": "user configures schema",
      "when": "validating input",
      "then": "value should meet schema requirements",
      "validation": {
        "setup": {
          "fieldConfig": {
            "name": "example_field",
            "type": "text"
          }
        },
        "assertions": [
          {
            "description": "value should meet schema requirements",
            "validateConfig": true,
            "expectError": "validation error"
          }
        ]
      }
    },
    {
      "id": "APP-FIELD-JSON-SCHEMA-002",
      "given": "schema is set",
      "when": "processing configuration",
      "then": "value should be used correctly",
      "validation": {
        "setup": {
          "fieldConfig": {
            "name": "example_field",
            "type": "text"
          }
        },
        "assertions": [
          {
            "description": "value should be used correctly",
            "validateConfig": true,
            "expectError": "validation error"
          }
        ]
      }
    }
  ]
}
