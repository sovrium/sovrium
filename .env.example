# Sovrium CLI Environment Variables Example
# Copy this file to .env and customize the values

# Application Configuration (Required)
# App schema containing configuration (name, description, etc.)
# Supports: inline JSON, inline YAML, or remote URL (http/https)
# SOVRIUM_APP_SCHEMA='{"name":"My Application","description":"A modern fullstack TypeScript application"}'
# SOVRIUM_APP_SCHEMA='name: My Application\ndescription: A modern fullstack TypeScript application'
# SOVRIUM_APP_SCHEMA='https://example.com/app-config.yaml'

# Server Configuration (Optional)
# PORT=3000
# BASE_URL=http://localhost:3000

# Database Configuration (Optional)
# PostgreSQL connection string
# If not set, server runs without database (metadata-only mode)
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# Authentication Configuration (Optional - if auth is enabled in JSON schema)
# Secret key for signing tokens and cookies (required for production)
# Generate with: openssl rand -base64 32
# AUTH_SECRET=your-secret-key-here

# Default Admin User (Optional - created on first startup if provided)
# AUTH_ADMIN_EMAIL=admin@example.com
# AUTH_ADMIN_PASSWORD=secure-admin-password
# AUTH_ADMIN_NAME=System Administrator

# OAuth Provider Credentials (Optional - only needed if providers enabled in JSON)
# Google OAuth
# GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=your-google-client-secret

# GitHub OAuth
# GITHUB_CLIENT_ID=your-github-client-id
# GITHUB_CLIENT_SECRET=your-github-client-secret

# Microsoft OAuth
# MICROSOFT_CLIENT_ID=your-microsoft-client-id
# MICROSOFT_CLIENT_SECRET=your-microsoft-client-secret

# Slack OAuth
# SLACK_CLIENT_ID=your-slack-client-id
# SLACK_CLIENT_SECRET=your-slack-client-secret

# GitLab OAuth
# GITLAB_CLIENT_ID=your-gitlab-client-id
# GITLAB_CLIENT_SECRET=your-gitlab-client-secret

# Email Configuration (Optional)
# SMTP server settings for sending emails
# If not set, falls back to Ethereal test SMTP in development
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM=noreply@yourdomain.com
# SMTP_FROM_NAME=Your App Name

# Note: E2E tests use Mailpit (localhost:1025) via test fixtures, not Ethereal
